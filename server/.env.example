# Порт, на котором слушает бэкенд
PORT=8787

# Telegram ID владельца салона — только у него будет доступ к /admin
ADMIN_TELEGRAM_ID=8419316772

# Список разрешённых origin через запятую для CORS.
# Если бэкенд отдаёт и фронтенд (тот же origin), можно оставить пустым.
# Пример: https://myapp.example.com,https://t.me
ALLOWED_ORIGINS=
